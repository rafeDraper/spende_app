<% @meetings_lists.each do |meetings_list| %>
  <div class="index_row clearfix">
    <h2 class="meetings_list_title"><%= link_to meetings_list.title, meetings_list %></h2>
    <p class="meetings_list_sub_title"> <%= meetings_list.responsible %></p>
  </div>
<% end %>

<div class="links">
  <%= link_to  "New meetings list", new_meetings_list_path %>
</div>

